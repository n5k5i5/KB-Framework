[project]
name = "kb-osint"
version = "0.1.0"
description = "KB-OSINT modüler OSINT sistemi (iskelet)"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"